services:
  rag-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - rag-processed:/app/data/processed
      - rag-logs:/app/data/logs

volumes:
  rag-processed:
  rag-logs:
